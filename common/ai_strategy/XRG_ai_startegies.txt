XRG_isbad = {
	allowed = {
		tag = XRG
	}
	enable = {
		country_exists = ZDG 
	}
	abort_when_not_enabled = yes
	ai_strategy = {
	type = front_unit_request
	tag = ZDG
 	value = 9999
    }
	ai_strategy = {
	type = front_unit_request
	tag = KQG
 	value = -5000
    }
	ai_strategy = {
	type = front_unit_request
	tag = SUM
 	value = -5000
    }
	ai_strategy = {
	type = front_unit_request
	tag = RTG
 	value = -5000
    }
	ai_strategy = {
	type = front_unit_request
	tag = LYU
 	value = -5000
    }
}

XRG_is_good = {
	allowed = {
		tag = XRG
	}
	enable = {
		has_global_flag = Liyue_Civil_War 
	}
	abort_when_not_enabled = yes
	ai_strategy = {
	type = front_unit_request
	tag = SUM
 	value = -5000
    }
}