XRG_isbad = {
	allowed = {
		tag = XRG
	}
	enable = {
		country_exists = ZDG 
	}
	ai_strategy = {
	type = front_unit_request
	tag = ZDG
 	value = 9999
    }
	ai_strategy = {
	type = front_unit_request
	tag = KQG
 	value = -5000
    }
	ai_strategy = {
	type = front_unit_request
	tag = SUM
 	value = -5000
    }
	ai_strategy = {
	type = front_unit_request
	tag = RTG
 	value = -5000
    }
	ai_strategy = {
	type = front_unit_request
	tag = LYU
 	value = -5000
    }
}