ZDG_is_very_good = {
	allowed = {
		tag = ZDG
	}
	enable = {
		has_global_flag = Liyue_Civil_War
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
	type = front_unit_request
	tag = SUM
 	value = -5000
    }
	
	ai_strategy = {
	type = front_unit_request
	tag = SNE
 	value = -5000
    }
	
	ai_strategy = {
	type = role_ratio
	id = infantry
	value = 99000
    }
	
	ai_strategy = {
	type = build_army
	id = infantry
	value = 5000
    }
	
	ai_strategy = {
	type = build_army
	id = motorized
	value = -9000
    }
	
	ai_strategy = {
	type = role_ratio
	id = armor
	value = -9000
    }
	ai_strategy = {
		type = equipment_production_min_factories
		id = artillery
		value = 30
	}
	
	ai_strategy = {
		type = equipment_production_min_factories
		id = infantry
		value = 50
	}
	
	ai_strategy = {
		type = equipment_production_min_factories
		id = support
		value = 30
	}
}

ZDG_tank_xianren = {
    allowed = {
		tag = ZDG
	}
	enable = {
		country_exists = XRG
		NOT = { XRG = { is_subject_of = ZDG } }
	}
	abort = {
		or = {
		NOT = { country_exists = XRG }
		XRG = { is_subject_of = ZDG }
		}
	}
	ai_strategy = {
	type = front_control
	tag = XRG  
	ratio = 0.1
	priority = 100  
	ordertype = front  
	execution_type = rush_weak  
	manual_attack = yes
	execute_order = yes
    }
}

ZDG_tank_xiao = {
    allowed = {
		tag = ZDG
	}
	enable = {
		country_exists = XOG
		NOT = { XOG = { is_subject_of = ZDG } }
	}
	abort = {
		or = {
		NOT = { country_exists = XOG }
		XOG = { is_subject_of = ZDG }
		}
	}
	ai_strategy = {
	type = front_control
	tag = XOG  
	ratio = 0.1
	priority = 100  
	ordertype = front  
	execution_type = rush_weak  
	manual_attack = yes
	execute_order = yes
    }
}